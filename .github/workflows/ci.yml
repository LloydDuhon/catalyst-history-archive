name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  lint:
    name: ruff
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v6

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: etl/requirements.txt

      - name: Install ruff
        run: python -m pip install --upgrade pip ruff==0.6.9

      - name: ruff check
        working-directory: etl
        run: ruff check .

      - name: ruff format --check
        working-directory: etl
        run: ruff format --check .

  validate-schemas:
    name: validate JSON Schemas
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v6

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: etl/requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r etl/requirements.txt

      - name: Check schemas are themselves valid
        run: |
          python - <<'PY'
          import json, sys
          from pathlib import Path
          from jsonschema import Draft202012Validator

          schemas = Path("schemas").glob("*.schema.json")
          failed = False
          for s in sorted(schemas):
              with s.open() as fh:
                  schema = json.load(fh)
              try:
                  Draft202012Validator.check_schema(schema)
                  print(f"OK   {s}")
              except Exception as exc:
                  print(f"FAIL {s}: {exc}")
                  failed = True
          sys.exit(1 if failed else 0)
          PY

      - name: Validate dataset against schemas
        run: python etl/validators/validate_against_schema.py

  verify-dashboard-data:
    name: verify dashboard data reproducibility
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v6

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v6
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: etl/requirements.txt

      - name: Regenerate dashboard data
        run: |
          python etl/scripts/generate_treasury_dashboard_data.py --repo-root . --out /tmp/treasury-dashboard-data
          cp site/funding-priorities-data.json /tmp/funding-priorities-data.json
          cp -a reports/funding-priorities-vision-2030 /tmp/funding-priorities-report
          python etl/scripts/generate_funding_priorities_report.py --repo-root .

      - name: Compare committed dashboard data
        run: |
          diff -u site/data.json /tmp/treasury-dashboard-data/data.json
          diff -u site/funding-priorities-data.json /tmp/funding-priorities-data.json
          diff -ru reports/funding-priorities-vision-2030 /tmp/funding-priorities-report
